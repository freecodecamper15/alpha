<%= form_for @author, :url => post_author_path(@post), :method => :patch do |f| %>
  <%= f.label :name, "Name" %>
  <%= f.text_field :name %>

  <%= f.submit 'Edit Confirmed' %>
<% end %>

<%= link_to 'Back', post_author_path(@post) %>
