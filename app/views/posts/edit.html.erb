<%= form_for @post, :url => post_path(@post), :method => :patch do |f| %>
  <%= f.label :title, 'Title' %>
  <%= f.text_field :title %>

  <%= f.label :content, 'Content' %>
  <%= f.text_area :content %>

  <%= f.submit "Update post" %>
<% end %>