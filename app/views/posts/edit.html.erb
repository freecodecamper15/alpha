<%= form_for @post, :url => post_path(@post), :method => :patch do |f| %>
  <%= f.label :title, 'Title' %>
  <%= f.text_field :title %>

  <%= f.label :comment, 'Comment' %>
  <%= f.text_area :comment %>

  <%= f.submit "Update post" %>
<% end %>